<%= simple_form_for :comment, url: comments_path , remote: true, format: :json, html: { class: 'nav nav-pills pull-right comment_form' } do |f| %>
  <%= f.text_field :desc, label: false, placeholder: 'Comment', size: "100", maxlength: "100" %>
  <%= f.button :submit %>
<% end %>

